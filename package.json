{
  "name": "color-palette-extractor",
  "version": "1.0.0",
  "description": "Image color palette extractor - Cloudflare Worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy dist/",
    "build": "wrangler deploy --dry-run --outdir dist",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
